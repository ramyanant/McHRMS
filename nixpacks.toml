[phases.setup]
nixPkgs = ["python312", "postgresql"]

[phases.install]
cmds = [
  "python3 -m venv /app/.venv",
  "/app/.venv/bin/pip install flask==3.1.0 psycopg2-binary==2.9.9 xhtml2pdf==0.2.17 svglib==1.5.1"
]

[start]
cmd = "/app/.venv/bin/python api/v2/main.py $PORT"
