[phases.setup]
nixPkgs = ["python312", "postgresql"]

[phases.install]
cmds = [
  "python3 -m venv /app/.venv",
  "/app/.venv/bin/pip install flask==3.1.0 psycopg2-binary==2.9.9"
]

[start]
cmd = "/app/.venv/bin/python api/app.py $PORT"
